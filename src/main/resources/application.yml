server:
    port: ${PORT:5000}
    servlet: 
        context-path: /
        
# OAuth2 Environment Variables
oauth2-config:
    oauth2-server-url: ${OAUTH2_SERVER_URL:https://development-oauth-server.herokuapp.com}
    oauth2-client-id: ${OAUTH2_CLIENT_ID:bussola-contabil-client}
    oauth2-client-secret: ${OAUTH2_CLIENT_SECRET:bussola-contabil-secret}

# Sentry Environment Variables
sentry-config:
    sentry-dsn: ${SENTRY_DSN}
    
spring: 
    datasource:
        url: jdbc:postgresql://ec2-54-243-253-24.compute-1.amazonaws.com:5432/db3hmvsfad1ru0?useSSL=true&ssl=true&sslfactory=org.postgresql.ssl.NonValidatingFactory
        username: oflbkrmqrgjcii
        password: 075a46e7517919940993e7ab7c74fb7aa92a910a7b6c419b7b60231d08e7cbdc

    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                temp:    
                    use_jdbc_metadata_defaults: false

    servlet:
        multipart:
            enabled: true
            file-size-threshold: 2KB
            max-request-size: 215MB
            max-file-size: 200MB
    

security:
    oauth2:
        client:
            clientId: ${oauth2-config.oauth2-client-id}
            clientSecret: ${oauth2-config.oauth2-client-secret}
            accessTokenUri: ${oauth2-config.oauth2-server-url}/oauth/token
            userAuthorizationUri: ${oauth2-config.oauth2-server-url}/oauth/authorize

        resource:
            userInfoUri: ${oauth2-config.oauth2-server-url}/user/info
            tokenInfoUri: ${oauth2-config.oauth2-server-url}/oauth/check_token
            filter_order: 3
            


## MULTIPART (MultipartProperties)
# Enable multipart uploads
# spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
# spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
# spring.servlet.multipart.max-file-size=200MB
# Max Request Size
# spring.servlet.multipart.max-request-size=215MB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
# file.upload-dir=./uploads